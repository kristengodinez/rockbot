package main

// Multiply each digit, alternating between 2 and 1
// If product is >= 10, then add the two digits
// Sum the results
// If sum % 10 == 0, then it is valid

func Luhn(number string) bool {
	return false
}
